import { BaseCommand } from "./BaseCommand";
export declare class SupportCommand extends BaseCommand {
    support(options: {
        json?: boolean;
        languages?: boolean;
        beautifiers?: boolean;
        all?: boolean;
    }): Promise<void>;
    private jsonPrinter;
    private listPrinter;
}
